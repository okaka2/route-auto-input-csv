/**
 * 初回起動時にだけ作る、初期設定のラベル。
 * 他のラベルは設定画面から自由に追加・削除できる(ここはあくまで最初の1回だけの
 * 初期値で、削除すれば消え、再起動しても復活しない)。
 *
 * 「利用者」は、他の画面では使わない業種特有の言葉として禁止語チェック
 * (tests/wording.test.ts)の対象だったが、このCSV版の初期ラベルとしては
 * 使ってよいことになっている(解禁済み)。
 */
export const DEFAULT_LABELS: readonly string[] = ['利用者', '従業員', 'パート'];
